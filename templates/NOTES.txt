═══════════════════════════════════════════════════════════════════════════════
  Successfully deployed: {{ include "common.names.fullname" . }}
═══════════════════════════════════════════════════════════════════════════════

Chart:       {{ .Chart.Name }}
Version:     {{ .Chart.Version }}
App Version: {{ .Chart.AppVersion }}
Namespace:   {{ .Release.Namespace }}
Release:     {{ .Release.Name }}

** Please wait while the application is being deployed **

───────────────────────────────────────────────────────────────────────────────
📡 SERVICE ACCESS
───────────────────────────────────────────────────────────────────────────────

Internal DNS (within cluster):
{{- if and .Values.service.ports (gt (len .Values.service.ports) 0) }}
  {{ include "common.names.fullname" . }}.{{ .Release.Namespace }}.svc.{{ .Values.clusterDomain }}:{{ (index .Values.service.ports 0).port }}
{{- else }}
  {{ include "common.names.fullname" . }}.{{ .Release.Namespace }}.svc.{{ .Values.clusterDomain }}
{{- end }}

{{- if .Values.ingress.enabled }}

External Access (via Ingress):
{{- if .Values.ingress.hostname }}
  URL: http{{ if .Values.ingress.tls }}s{{ end }}://{{ .Values.ingress.hostname }}
{{- else }}
  Check ingress hosts with:
  kubectl get ingress --namespace {{ .Release.Namespace }} {{ include "common.names.fullname" . }}
{{- end }}
{{- else }}

External Access (via {{ .Values.service.type }}):
{{- if contains "LoadBalancer" .Values.service.type }}

  NOTE: It may take a few minutes for the LoadBalancer IP to be available.

  Watch the status:
    kubectl get svc --namespace {{ .Release.Namespace }} -w {{ include "common.names.fullname" . }}

  Get the URL:
    export SERVICE_IP=$(kubectl get svc --namespace {{ .Release.Namespace }} {{ include "common.names.fullname" . }} -o jsonpath='{.status.loadBalancer.ingress[0].ip}')
    export SERVICE_PORT=$(kubectl get svc --namespace {{ .Release.Namespace }} {{ include "common.names.fullname" . }} -o jsonpath='{.spec.ports[0].port}')
    echo "Application URL: http://${SERVICE_IP}:${SERVICE_PORT}"

{{- else if contains "ClusterIP" .Values.service.type }}

  Port-forward to access locally:
    export SERVICE_PORT=$(kubectl get svc --namespace {{ .Release.Namespace }} {{ include "common.names.fullname" . }} -o jsonpath='{.spec.ports[0].port}')
    kubectl port-forward --namespace {{ .Release.Namespace }} svc/{{ include "common.names.fullname" . }} ${SERVICE_PORT}:${SERVICE_PORT}

  Then access at: http://127.0.0.1:${SERVICE_PORT}

{{- else if contains "NodePort" .Values.service.type }}

  Get the NodePort and Node IP:
    export NODE_PORT=$(kubectl get svc --namespace {{ .Release.Namespace }} {{ include "common.names.fullname" . }} -o jsonpath='{.spec.ports[0].nodePort}')
    export NODE_IP=$(kubectl get nodes -o jsonpath='{.items[0].status.addresses[0].address}')
    echo "Application URL: http://${NODE_IP}:${NODE_PORT}"

{{- end }}
{{- end }}

───────────────────────────────────────────────────────────────────────────────
🔍 STATUS & MONITORING
───────────────────────────────────────────────────────────────────────────────

Check deployment status:
  kubectl get pods --namespace {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "common.names.name" . }}

View logs:
  kubectl logs --namespace {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "common.names.name" . }} --tail=100 -f
{{- if .Values.metrics.enabled }}

Metrics endpoint:
  {{ include "common.names.fullname" . }}.{{ .Release.Namespace }}.svc.{{ .Values.clusterDomain }}:{{ .Values.metrics.service.port }}{{ .Values.metrics.path }}
{{- if .Values.metrics.serviceMonitor.enabled }}

Prometheus ServiceMonitor: ✓ Enabled
  Your metrics will be automatically scraped by Prometheus Operator
{{- end }}
{{- if .Values.prometheusRule.enabled }}

Prometheus Alerts: ✓ Enabled
  Custom alerts are configured in PrometheusRule
{{- end }}
{{- end }}

───────────────────────────────────────────────────────────────────────────────
📚 RESOURCES
───────────────────────────────────────────────────────────────────────────────

Chart documentation:
  https://github.com/okassov/universal-chart

Report issues:
  https://github.com/okassov/universal-chart/issues

Artifact Hub:
  https://artifacthub.io/packages/helm/okassov/universal

═══════════════════════════════════════════════════════════════════════════════